/* Bismillahir Rohmanir Roheem
   In the Name of Allah (SWT) the Merciful the Compassionate

   Rakibul Hassan (razorrakib)
   BSc CSE - American International University Bangladesh
*/

#include <bits/stdc++.h>
using namespace std;

int main()
{
    /*
        set precision : to set the exact number of digits after a float/double
        use #include<iomanip>
        cout << fixed << setprecision(n) << float/double var
    */

    float x = 1.45676;
    cout << fixed << setprecision(3) << x;

    return 0;
}